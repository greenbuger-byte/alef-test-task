import React from 'react';
import Default from "@alef/layers/default";

const NotFound = () => {
    return (
        <Default title="Страница не найдена">
          404  СТРАНИЦА НЕ НАЙДЕНА
        </Default>
    );
};

export default NotFound;